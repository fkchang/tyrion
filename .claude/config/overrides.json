{
  "pre-push": {
    "test": {
      "command": "ruby -Ilib -e 'Dir[\"test/test_*.rb\"].each { |f| require File.expand_path(f) }; require \"minitest/autorun\"' && bundle exec rspec",
      "name": "Tests (Minitest + RSpec)",
      "required": true
    }
  }
}
